# 🤖 Web AGENTS.md

## 局部规则 (Local Rules)
当在 `web/` 目录下工作时，遵守以下 Web 前端开发约定：

1. **框架栈**: React + TypeScript + Vite。
2. **架构模式**: 按 Feature 组织代码 (Feature-Sliced Design 的变体)。
3. **状态管理**: 优先局部状态，跨组件使用 React Context 或 Zustand。
4. **样式**: 使用 TailwindCSS (或指定的 CSS-in-JS)。
